#include <stdlib.h>
#include <unistd.h>

mapper(){

}

int main(){
	int BUFSZ = 250
	char buffer[BUFSZ]
	char * const argv[] =
	child = [];
	int pid = 1;
	int pfd[2]
	int i;

	for(i=0; i<num_maps; i++){
		if(pid != 0){
			pid = fork()
		}
	}
	if(pid>0){

	}

	if(pid == 0){
		if(read(pfd[0]), buffer, BUFSZ) == -1){
			perror("read")
		}
		int frequency = Frequency()
		if(write(pfd[1]), frequency) == -1{
			perror("write")
		}
		printf("%d written\n", frequency)
	}
}